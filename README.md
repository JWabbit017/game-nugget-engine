IMPORTANT NOTICE FOR GNE v0.1^ :
Using the dist is currently not recommended.
The default minification Webpack applies has caused some issues. Using src/js/init.js is the stable option until I can work out a different bundler.
